# clause-library has no external dependencies or secrets. CLAUSE_STORE_PATH
# (default /data/clause-library.json) is where it keeps its records; on first
# start it seeds the standard clauses from the bundled clauses.json.
